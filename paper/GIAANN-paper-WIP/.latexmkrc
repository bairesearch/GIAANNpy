# Ensure BibTeX runs for natbib bibliographies.
# (Prism uses latexmk; this nudges it to use BibTeX when needed.)
$bibtex_use = 1;

# Keep auxiliary files (default). Uncomment if you want latexmk to clean.
# $cleanup_mode = 1;